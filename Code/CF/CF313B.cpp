#include <bits/stdc++.h>
#define ll long long
using namespace std;

void solve() {
  string s;
  cin >> s;
  int m;
  vector<vector<int>> v(m, vector<int>(2));
  for(int i = 0; i < m; ++i)
    cin >> v[i][0] >> v[i][1];
}

int main() {
  std::ios::sync_with_stdio(false), cin.tie(0);
  solve();
}